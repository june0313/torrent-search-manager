import React from 'react';
import TorrentSearchManager from './TorrentSearchManager';

class App extends React.Component {
    render() {
        return (
            <TorrentSearchManager />
        );
    }
}

export default App;